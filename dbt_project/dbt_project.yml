name: metal_analytics
version: "1.0.0"
profile: metal_analytics

model-paths: ["models"]
macro-paths: ["macros"]

models:
  metal_analytics:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
